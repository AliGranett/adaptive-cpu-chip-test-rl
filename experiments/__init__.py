"""Standalone experiment drivers that build on top of the ``src`` package.

These scripts orchestrate multi-run experiments (training sweeps, aggregation,
Pareto analysis and plotting) without modifying the core library code.
"""
